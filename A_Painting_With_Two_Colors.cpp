#include<bits/stdc++.h>
using namespace std;

int main(){
  int t;
  cin>>t;
  while (t--)
  {
    int n,a,b;
    cin>>n>>a>>b;
    bool isN = (n%2==0) ? true:false;
    long long sum = (a+b)-b;
    bool isS = (sum%2==0) ? true:false;
    if(isN && isS ) 
  }
  
}